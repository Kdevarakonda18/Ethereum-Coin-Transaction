<nav class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 shadow-md text-white">
  <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
    
    <!-- Brand -->
    <a href="<%= isLoggedIn ? '/dashboard' : '/' %>" class="flex items-center gap-2 text-2xl font-bold tracking-wide hover:scale-105 transform transition">
      <img src="/images/ether-logo.png" alt="ETH" class="w-6 h-6">
      EtherPay
    </a>

    <!-- Navigation -->
    <div class="flex items-center gap-4">
      <% if (isLoggedIn) { %>
        <!-- Links -->
        <a href="/dashboard" class="hover:bg-white hover:text-indigo-700 px-4 py-1 rounded transition">Dashboard</a>
        <a href="/transactions" class="hover:bg-white hover:text-indigo-700 px-4 py-1 rounded transition">Transactions</a>
        <a href="/settings" class="hover:bg-white hover:text-indigo-700 px-4 py-1 rounded transition">Settings</a>

        <!-- Connect Wallet Button -->
        <button id="connectWalletBtn" class="bg-white text-indigo-700 font-semibold px-3 py-1 rounded hover:bg-indigo-100 transition">
          Connect Wallet
        </button>

        <!-- Avatar Dropdown -->
        <div class="relative group">
          <button class="focus:outline-none">
            <img src="<%= user?.avatar || '/default-avatar.png' %>" alt="Avatar" class="w-10 h-10 rounded-full border-2 border-white object-cover shadow">
          </button>

          <!-- Dropdown -->
          <div class="absolute right-0 mt-2 w-64 bg-white text-black rounded-lg shadow-lg border hidden group-hover:block z-50 p-4 text-sm">
            <div class="text-center mb-3">
              <img src="<%= user?.avatar || '/default-avatar.png' %>" class="w-16 h-16 mx-auto rounded-full border border-indigo-500 shadow" />
              <p class="mt-2 font-semibold text-gray-800"><%= user?.email %></p>
              <p class="text-gray-500 text-xs break-all"><%= user?.walletAddress || 'Not Connected' %></p>
            </div>

            <a href="/profile" class="block text-center bg-indigo-100 text-indigo-700 py-2 rounded mb-2 hover:bg-indigo-200 transition">
              Edit Profile
            </a>

            <form action="/logout" method="POST">
              <button type="submit" class="w-full bg-red-500 text-white py-2 rounded hover:bg-red-600 transition">
                🔓 Logout
              </button>
            </form>
          </div>
        </div>

      <% } else { %>
        <!-- Not Logged In -->
        <a href="/login" class="hover:bg-white hover:text-indigo-700 px-4 py-1 rounded transition">Login</a>
        <a href="/signup" class="hover:bg-white hover:text-indigo-700 px-4 py-1 rounded transition">Signup</a>
      <% } %>
    </div>
  </div>
</nav>
